#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

ll arr[200001];
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        for (int i = 0; i < n; i++)
            cin>>arr[i];
        sort(arr, arr + n);
        ll l = -1, r = 1'000'000'000;
        

    }
    return 0;
}